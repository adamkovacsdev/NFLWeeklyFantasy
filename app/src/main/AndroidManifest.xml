<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.adamkovacs.nflweeklyfantasy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.EditUserActivity" android:label="Edit user"></activity>
        <activity android:name=".Activities.WeeklyPickEmActivity" android:label="Weekly Pick'Em"/>
        <activity android:name=".Activities.LeaderboardActivity" android:label="Leaderboard"/>
        <activity android:name=".Activities.MainActivity" android:label="NFL Weekly Fantasy">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>