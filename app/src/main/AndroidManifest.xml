<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.adamkovacs.nflweeklyfantasy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.EditUserActivity" android:label="Edit user"></activity>
        <activity android:name=".Activities.WeeklyPickEmActivity" android:label="Weekly Pick'Em"/>
        <activity android:name=".Activities.LeaderboardActivity" android:label="Leaderboard"/>
        <activity android:name=".Activities.MainScreenActivity" android:label="NFL Weekly Fantasy"/>
        <activity android:name=".Activities.SignUpActivity" android:label="Sign up"/>
        <activity android:name=".Activities.MainActivity" android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>